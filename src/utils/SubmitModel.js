const submitModel = class SubmitModel {
  constructor (result, id, shopName, shopId, turn) {
    this.result = result
    this.id = id
    this.shopName = shopName
    this.shopId = shopId
    this.turn = turn
  }
}

export default submitModel
